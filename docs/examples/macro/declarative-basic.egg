pub macro repeat-expr(expression: expr, times: const u64 = 2) -> expr
return match times
    case 0 then panic 'Cannot repeat for 0 times'
    case 1 then expression
    case _ then %%%
        do
            $expression
            repeat-expr!($expression, $(times - 1))
